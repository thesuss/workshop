<% if @courses.any? %>
  <% @courses.each do |course| %>
    <h3><%= course.id  %> &nbsp;
    <%= course.title %></h3>
    <p><%= course.description  %></p>
  <% end %>
<% else %>
<h1>Det finns inga kategorier</h1>
<% end %>
<%= link_to 'Create course', '/courses/create' %>
